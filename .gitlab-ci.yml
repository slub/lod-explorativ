default:
  image: node:14-alpine
  before_script:
    - npm install

deploymenttime:
  script:
    - export
    - yarn build
    - rsync -r --delete ${CI_PROJECT_DIR}/public /var/www/webapp_main
  tags:
    - search-deployment

